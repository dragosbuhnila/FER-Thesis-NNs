------------------------------------------------------------------------------------------------------------------------------
EXTRA) Move the occlusion logic inside of the training so that you can control the occlusion ratio during the run just like you do with learning ratio backoff
	- you may do that by either making the occlusion an actual augmentation object with a controllable_during_epochs parameter (i.e. the occ ratio)
	- or by making it a layer?
			> or could xor with a mask which is unique for each image
			> which could either just load a different image just like an augmentation object would do
	






=========================================== AFTER MEETING TASKS PRIORITIES ==================================
3> DEVELOP xai
	> BUBBLES
		- modify the adele extracted images names to include the index in the dataset (i.e. from 0 to 350)
			 the format should be so that the following works:
					if f"{image_name}__predictedclass" in image_emo:(where image_emo is image_9__predictedclass_ANGRY_withprob_66.74_avgbubblesnum_15.67.png)
	> EXTERNAL
	> GRADCAM
		- 
4> DEVELOP Yolo

others:
rewrite bubbles functions to process whole batch before running everything, now it's too slow
... RUN csm bubbles on the two new configurations
... SAVE training graphs from models of interest as soon as the mlruns is zipped (hope it works)
STUDY models in detail



ISSUES:
check why my agreement on federica models on original test set is different from her paper's 
> maybe it's just because i don't have yolo yet


Create correspondence between images in occluded test set and those in original test set to have base images for landmarker
	> there may be an issue with the index thing in do_csm, because I'm not sure that image n1 from the dataloader is actually the exact first image in my images folder. Do a check using hashes and maybe just rename them to also include image_idx at the beginning, and just add a __ again do split the filename in 3 total parts
	
	
	

failed gradcams
	- occft_pattlitle
		> sequential models
		> accuracy 0.3 ????? why ?????
		> [ERROR] Failed to initialize GradCAM for model occft_pattlite: bad marshal data (unknown type code)
			
			
layers: accuracies all ok
	- convnext:		31 expected	31 found	OK
	- efficientnet	16 expected 16 found	OK
	- inception 	40 expected 40 found	OK
	- pattlite		9  expected 18 found	HMM (removed SEQUENTIAL 1 out of 2)
	- resnet		13 expected 13 found 	OK
	- vgg			12 expected 12 found	OK 	(removed SEQUENTIAL 2 out of 2)
	
	
	
> Download zip after checkin size






next meeting:
> check model results by comparing architectures with those on literature review from a year ago
	OK 1/2: 
> controlla origine dei paper se sono su conferenze o solo arxiv
	OK 7/7
> guarda paper ricevuto via mail della Marcolin e trova connessioni tra cervello e CNN
	OK 0/2
> valutare raggruppo di accuracy metodo
	OK 1/1: just round
> left/right analysis instead of ROI
	OK 2/2: analyze and write some lines
> RUN meandif, statistical granular, statistical folders
	OK 4/4: analyze results	   CORRECT: USE DW9 INSTEAD OF PW8 IN PATTLITE/MOBILENET ---- NOOOO, idk why just use pw like fede
		- there's some blocks where it should be white (empty) but isn't
			> BEST_VS_WORST
			> YOLO_BUB VS REBLEO
				this has even a really big value! (OK THIS IS ACTUALLY NORMAL BC A BLANK IMAGE WAS KEPT AND THUS THE DIFFERENCE WILL BE ALMOST TOTAL. THE FACT THAT ITS NOT 1 MEANS THAT PROBABLY THERE'S REALLY FEW PIXELS WHICH WE SHOULD JUST THROW AWAY (CHECK IF IT'S BECAUSE OF NEGATIVE MASKS, PROBABLY YES)
			> YOLO_EXT VS REBLEO
		- also, put all gradcams for a specific model in a shared thingy so that I can see things
			> like for Efficient net, gradcam is all over the place, like, last layers scan vertically, and first layer have some full image thing
			
			
			
perche usare movilenet1 e non i piu nuovi? Perche lo usa pattlite
change all graphs showing pattlite to mobilenet
confirm our CNNs are deterninistic and gradcam too (since others arent)
check if by any chance the confusion matrices of gradcam xai aren't transposed
ragiona su perche dovrebbe essereci comportamento riga/colonna in primi/ultimi layer
vai a cercare il paper sul label domain shift

valuta question canoniche per match/mismatch e valuta la questione dei diversi training per una sola rete per vederle come individui diversi e farne gruppi?

extra: run xais with occft models on old test set and run again lr, to see if lr depends on test set or on training


do bosphorus images appear in the training set????

