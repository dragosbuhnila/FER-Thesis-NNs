# Requirements
python -m pip freeze > pipfreeze_requirements.txt
conda list > condalist_requirements.txt


# Delete Conda Env
conda deactivate
conda env remove -n fer-thesis -y
conda env list


# find free gpus on cluster
squeue --format="%.18i %.9P %.8j %.8u %.2t %.10M %.6D %R %G"
sinfo --format="%P %G"