OKAY > scaricare tutta la roba necessaria dall'hpc


TODO > match vs mismatch e 12 tipi di occlusione (totale 14 subsets)
	OKAY > creare i 14 dataset separati
	OKAY > runnare ConfMat analysis per i 7 modelli sui 14 dataset separatamente
	TODO > runnare i 3 XAI methods per i 7 modelli sui 2 dataset separatamente
		OKAY > modificare codici XAI e CSM per accettare i 14 dataset tramite opzione
		OKAY > controllare che la prima run, almeno per convnext, sia funzionante
		TOFIX > fixare codice nuovo (manca image_idx nei paths)
	TODO > runnare l'estrazione di ROI-vectors per le XAI
	TODO > runnare la comparazione numerica con gli umani
	
TODO > canonical_faces_occluded
	OKAY > create them
	TODO > modify code to use them
	
	
TODO > scrivere documentazione sul lavoro di aggregazione di reti dati training diversi siccome il training è stocastico


TODO > caricare cartelle di files utili
	masks repo:
		OKAY	backup
		TODO	recheck
	models repo:
		TODO	backup
					- mlruns
					- saved_images/older_ones (it contains XAI methods for non occluded models or datasets or combos
					- out_err
					- data/models/occft/...		(the models that you're interested in so probably just the 1.0 ratio and posonly)